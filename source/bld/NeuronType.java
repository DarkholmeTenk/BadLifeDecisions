package bld;

/**
 * Enum that registers the availiable neuron types for the NeuronFactory, any
 * new neurons should extend Neuron and register their type within this enum;
 * as well as have a creation method within the factory class.
 */
public enum NeuronType
{
  BASIC_NEURON
}
